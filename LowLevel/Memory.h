

 memchr() // Returns a pointer to the first byte in a block 
	  // matching the specified character
 memcmp() // Compares two blocks of memory, returning any
	  // differences
 memcpy() // Copies two nonoverlapping blocks of memory
 memmove() // Copies two blocks of memory, which may or may not
	   // overlap
 memset() // Fills a block of memory with the specified byte


